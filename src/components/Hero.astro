---
// Modern hero section with vibrant design - Mobile optimized

// Preload hero background image for better LCP
const bgPattern = '/assets/hero-pattern.webp';
---

<section class="relative overflow-hidden bg-gradient-to-br from-gray-50 via-white to-blue-50 dark:from-gray-900 dark:via-gray-800 dark:to-blue-900 py-12 sm:py-16 lg:py-20">
  <div class="absolute inset-0 bg-hero-pattern opacity-5 dark:opacity-10"></div>
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <h1 class="text-3xl sm:text-4xl lg:text-5xl xl:text-6xl font-bold tracking-tight text-gray-900 dark:text-white">
        Latest 
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600">
          MOD APKs
        </span>
      </h1>
      <p class="mt-4 sm:mt-6 text-base sm:text-lg leading-7 sm:leading-8 text-gray-600 dark:text-gray-300 max-w-2xl mx-auto px-4">
        Download the latest modified Android applications with premium features unlocked. Fast, secure, and always updated.
      </p>
      <div class="mt-8 sm:mt-10 flex flex-col sm:flex-row items-center justify-center gap-4 sm:gap-x-6 px-4">
        <a 
          href="#featured-apps" 
          class="w-full sm:w-auto rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-6 sm:px-8 py-2.5 sm:py-3 text-sm sm:text-base font-semibold text-white shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200 text-center"
        >
          Browse MODs
        </a>
        <a 
          href="/categories" 
          class="text-sm sm:text-base font-semibold leading-6 text-gray-900 dark:text-gray-100 hover:text-blue-600 dark:hover:text-blue-400 transition-colors duration-200"
        >
          View Categories <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .bg-hero-pattern {
    background-image: url('/assets/hero-pattern.webp');
    background-size: 400px 400px;
    background-repeat: repeat;
    will-change: transform;
  }
  
  /* Fallback for browsers that don't support webp */
  @supports not (background-image: url('/assets/hero-pattern.webp')) {
    .bg-hero-pattern {
      background-image: radial-gradient(circle at 1px 1px, rgba(0,0,0,0.15) 1px, transparent 0);
      background-size: 20px 20px;
    }
  }
</style>