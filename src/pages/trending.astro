---
export const prerender = true;

import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ProgressiveImage from '../components/ProgressiveImage.astro';
import { getAllApps } from '../lib/supabase.js';

// Fetch all apps and calculate trending score
let allApps = [];
try {
  allApps = await getAllApps();
} catch (error) {
  console.error('Error loading apps:', error);
  allApps = [];
}

// Calculate trending score (rating * votes + featured bonus)
const trendingApps = allApps
  .map(app => ({
    ...app,
    trendingScore: (app.rating * app.votes) + (app.is_featured ? 1000 : 0)
  }))
  .sort((a, b) => b.trendingScore - a.trendingScore);

const title = "Trending MOD APKs - DexAPK";
const description = "Discover the hottest and most popular MOD APKs trending right now with premium features unlocked.";
---

<Layout title={title} description={description}>
  <Header />
  
  <main class="min-h-screen bg-gray-50 dark:bg-gray-900">
    
    <!-- Breadcrumbs -->
    <nav class="bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700" aria-label="Breadcrumb">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <ol class="flex items-center space-x-2 py-4 text-sm" role="list">
          <li>
            <a href="/" class="text-gray-500 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors duration-200" aria-label="Go to homepage">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
              </svg>
              <span class="sr-only">Home</span>
            </a>
          </li>
          <li class="flex items-center">
            <svg class="w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
              <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
            </svg>
            <span class="ml-2 font-medium text-gray-900 dark:text-white" aria-current="page">Trending</span>
          </li>
        </ol>
      </div>
    </nav>

    <!-- Page Header -->
    <section class="bg-gradient-to-r from-red-500 via-pink-500 to-purple-600 py-12 sm:py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="flex items-center justify-center mb-6">
          <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center backdrop-blur-sm animate-pulse">
            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4">
          🔥 Trending MOD APKs
        </h1>
        <p class="text-xl text-pink-100 max-w-3xl mx-auto mb-6">
          Discover the hottest and most popular modified applications trending right now
        </p>
        <div class="flex flex-wrap items-center justify-center gap-6 text-pink-100">
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
              <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
            <span class="font-medium">Hot & Popular</span>
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span class="font-medium">Highly Rated</span>
          </div>
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span class="font-medium">{trendingApps.length} Trending Apps</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Trending Stats -->
    <section class="bg-white dark:bg-gray-800 py-8">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          
          <div class="text-center p-6 bg-gradient-to-r from-red-50 to-pink-50 dark:from-red-900/20 dark:to-pink-900/20 rounded-2xl">
            <div class="w-12 h-12 bg-red-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
              #{trendingApps.findIndex(app => app.is_featured) + 1 || 1}
            </h3>
            <p class="text-gray-600 dark:text-gray-400">Top Trending Position</p>
          </div>

          <div class="text-center p-6 bg-gradient-to-r from-pink-50 to-purple-50 dark:from-pink-900/20 dark:to-purple-900/20 rounded-2xl">
            <div class="w-12 h-12 bg-pink-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
              </svg>
            </div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
              {trendingApps.length > 0 ? trendingApps[0].rating : '4.5'}
            </h3>
            <p class="text-gray-600 dark:text-gray-400">Highest Rating</p>
          </div>

          <div class="text-center p-6 bg-gradient-to-r from-purple-50 to-blue-50 dark:from-purple-900/20 dark:to-blue-900/20 rounded-2xl">
            <div class="w-12 h-12 bg-purple-500 rounded-xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
              {trendingApps.reduce((sum, app) => sum + app.votes, 0).toLocaleString()}
            </h3>
            <p class="text-gray-600 dark:text-gray-400">Total Downloads</p>
          </div>

        </div>
      </div>
    </section>

    <!-- Search and Filters -->
    <section class="bg-gray-50 dark:bg-gray-900 py-6">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
          
          <!-- Search -->
          <div class="flex-1 max-w-md">
            <label for="search-trending" class="sr-only">Search trending apps</label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                <svg class="w-5 h-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
              </div>
              <input
                type="text"
                id="search-trending"
                class="w-full pl-10 pr-4 py-3 border border-gray-300 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-red-500 focus:border-red-500 dark:bg-gray-700 dark:text-white transition-colors duration-200"
                placeholder="Search trending MOD APKs..."
                aria-label="Search for trending MOD APKs"
              />
            </div>
          </div>

          <!-- Filters -->
          <div class="flex flex-wrap items-center gap-4">
            
            <!-- Category Filter -->
            <div>
              <label for="trending-category-filter" class="sr-only">Filter by category</label>
              <select
                id="trending-category-filter"
                class="px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-red-500 focus:border-red-500 dark:bg-gray-700 dark:text-white transition-colors duration-200"
                aria-label="Filter trending apps by category"
              >
                <option value="">All Categories</option>
                <option value="Productivity">Productivity</option>
                <option value="Music">Music</option>
                <option value="Video">Video</option>
                <option value="Entertainment">Entertainment</option>
                <option value="Social">Social</option>
                <option value="Photography">Photography</option>
                <option value="Games">Games</option>
                <option value="Media">Media</option>
                <option value="Apps">Apps</option>
              </select>
            </div>

            <!-- Trending Filter -->
            <div>
              <label for="trending-filter" class="sr-only">Filter by trending type</label>
              <select
                id="trending-filter"
                class="px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-red-500 focus:border-red-500 dark:bg-gray-700 dark:text-white transition-colors duration-200"
                aria-label="Filter by trending type"
              >
                <option value="">All Trending</option>
                <option value="featured">Featured Only</option>
                <option value="high-rated">High Rated (4.5+)</option>
                <option value="popular">Most Popular</option>
              </select>
            </div>

          </div>
        </div>
      </div>
    </section>

    <!-- Trending Apps Grid -->
    <section class="py-8 sm:py-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        
        <!-- Results Info -->
        <div class="flex items-center justify-between mb-6">
          <p class="text-gray-600 dark:text-gray-400" id="trending-results-count" aria-live="polite">
            Showing <span id="showing-trending-count">{trendingApps.length}</span> of <span id="total-trending-count">{trendingApps.length}</span> trending apps
          </p>
        </div>

        <!-- Apps Container -->
        <div id="trending-apps-container" class="grid grid-cols-2 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-4 sm:gap-6" role="grid" aria-label="Trending MOD APKs collection">
          
          {trendingApps.length > 0 ? trendingApps.map((app, index) => (
            <article 
              role="gridcell"
              class="trending-app-card group relative bg-white dark:bg-gray-800 rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-100 dark:border-gray-700 focus-within:ring-2 focus-within:ring-red-500 focus-within:ring-offset-2 dark:focus-within:ring-offset-gray-900"
              data-app-name={app.name}
              data-app-category={app.category}
              data-app-rating={app.rating}
              data-app-date={app.created_at}
              data-app-featured={app.is_featured}
              data-trending-position={index + 1}
            >
              <a 
                href={`/${app.slug}`} 
                class="block w-full h-full focus:outline-none"
                aria-label={`Download ${app.name} v${app.version} MOD APK - Trending #${index + 1} ${app.category} app with premium features unlocked`}
              >
                <div class="relative h-32 sm:h-40 bg-gray-100 dark:bg-gray-700 flex items-center justify-center overflow-hidden group-hover:scale-105 transition-transform duration-300">
                  <!-- Trending Position Badge -->
                  <div class="absolute top-2 left-2 flex gap-1.5 z-10">
                    <span class={`text-white text-xs font-bold px-2 py-0.5 rounded-full flex items-center gap-1 shadow-sm ${
                      index === 0 ? 'bg-yellow-500' : 
                      index === 1 ? 'bg-gray-400' : 
                      index === 2 ? 'bg-orange-600' : 
                      'bg-red-500'
                    }`}>
                      <svg class="w-2.5 h-2.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                        <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                      </svg>
                      #{index + 1}
                    </span>
                    {app.is_featured && (
                      <span class="bg-blue-500 text-white text-xs font-semibold px-2 py-0.5 rounded-full shadow-sm">
                        FEATURED
                      </span>
                    )}
                    <span class="bg-red-500 text-white text-xs font-semibold px-2 py-0.5 rounded-full shadow-sm">
                      HOT
                    </span>
                  </div>
                  
                  <!-- App Icon -->
                  <div class="w-20 h-20 sm:w-24 sm:h-24 bg-white dark:bg-gray-800 rounded-2xl sm:rounded-3xl flex items-center justify-center transition-colors duration-300 shadow-lg overflow-hidden">
                    {app.icon ? (
                      <img 
                        class="hidden"
                        src={app.icon} 
                        alt={`${app.name} icon`}
                        loading="lazy"
                        onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                      />
                      <ProgressiveImage 
                        src={app.icon} 
                        alt={`${app.name} icon`}
                        class="rounded-2xl sm:rounded-3xl"
                        aspectRatio="1/1"
                      />
                      <div class="w-12 h-12 sm:w-16 sm:h-16 text-gray-700 dark:text-gray-300 hidden items-center justify-center">
                        <svg class="w-full h-full" fill="currentColor" viewBox="0 0 24 24">
                          <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                        </svg>
                      </div>
                    ) : (
                      <svg class="w-12 h-12 sm:w-16 sm:h-16 text-gray-700 dark:text-gray-300" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                      </svg>
                    )}
                  </div>
                  
                  <!-- Hover Overlay -->
                  <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition-colors duration-300 flex items-center justify-center opacity-0 group-hover:opacity-100">
                    <div class="bg-white/20 backdrop-blur-sm rounded-full p-3 transform scale-0 group-hover:scale-100 transition-transform duration-300">
                      <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"/>
                      </svg>
                    </div>
                  </div>
                </div>
                
                <!-- App Details -->
                <div class="p-3 group-hover:bg-gray-50 dark:group-hover:bg-gray-700 transition-colors duration-300">
                  <h3 class="text-sm sm:text-base font-semibold text-gray-900 dark:text-white mb-1 truncate group-hover:text-red-600 dark:group-hover:text-red-400 transition-colors duration-300">
                    {app.name}
                  </h3>
                  <p class="text-xs sm:text-sm font-medium mb-2 text-red-600 dark:text-red-400 transition-colors duration-300">
                    {app.category} • Trending #{index + 1}
                  </p>
                  <div class="flex items-center justify-between text-xs text-gray-600 dark:text-gray-400">
                    <div class="flex items-center gap-1" title={`Rating ${app.rating}/5`}>
                      <svg class="w-3 h-3 text-yellow-500" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                      </svg>
                      <span class="text-xs">{app.rating}</span>
                    </div>
                    <div class="flex items-center gap-1" title={`${app.votes} downloads`}>
                      <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"/>
                      </svg>
                      <span class="text-xs">{app.votes > 1000 ? `${(app.votes/1000).toFixed(1)}k` : app.votes}</span>
                    </div>
                  </div>
                </div>
                
                <!-- Screen Reader Content -->
                <div class="sr-only">
                  Trending position: #{index + 1}.
                  Rating: {app.rating} out of 5 stars with {app.votes} downloads.
                  Last updated: {app.last_updated}.
                  Publisher: {app.publisher}.
                  Requirements: {app.requirements}.
                  {app.is_featured ? 'Featured app. ' : ''}
                  Click to download and view more details.
                </div>
              </a>
            </article>
          )) : (
            <div class="col-span-full text-center py-12" role="status" aria-live="polite">
              <div class="text-gray-500 dark:text-gray-400">
                <svg class="w-16 h-16 mx-auto mb-4" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
                <h3 class="text-lg font-medium mb-2">No trending apps available</h3>
                <p class="text-sm">Please connect to Supabase to load trending data.</p>
              </div>
            </div>
          )}

        </div>

      </div>
    </section>

  </main>
  
  <Footer />
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('search-trending');
    const categoryFilter = document.getElementById('trending-category-filter');
    const trendingFilter = document.getElementById('trending-filter');
    const appsContainer = document.getElementById('trending-apps-container');
    const showingCount = document.getElementById('showing-trending-count');
    const totalCount = document.getElementById('total-trending-count');
    
    let allApps = Array.from(document.querySelectorAll('.trending-app-card'));
    let filteredApps = [...allApps];
    
    // Initialize
    updateResultsCount();
    
    // Search functionality
    searchInput?.addEventListener('input', function() {
      filterApps();
    });
    
    // Category filter
    categoryFilter?.addEventListener('change', function() {
      filterApps();
    });
    
    // Trending filter
    trendingFilter?.addEventListener('change', function() {
      filterApps();
    });
    
    function filterApps() {
      const searchTerm = searchInput?.value.toLowerCase() || '';
      const selectedCategory = categoryFilter?.value || '';
      const selectedTrending = trendingFilter?.value || '';
      
      // Filter apps
      filteredApps = allApps.filter(app => {
        const appName = app.dataset.appName?.toLowerCase() || '';
        const appCategory = app.dataset.appCategory || '';
        const appRating = parseFloat(app.dataset.appRating) || 0;
        const appFeatured = app.dataset.appFeatured === 'true';
        const trendingPosition = parseInt(app.dataset.trendingPosition) || 999;
        
        const matchesSearch = !searchTerm || appName.includes(searchTerm);
        const matchesCategory = !selectedCategory || appCategory === selectedCategory;
        
        let matchesTrending = true;
        if (selectedTrending === 'featured') {
          matchesTrending = appFeatured;
        } else if (selectedTrending === 'high-rated') {
          matchesTrending = appRating >= 4.5;
        } else if (selectedTrending === 'popular') {
          matchesTrending = trendingPosition <= 10;
        }
        
        return matchesSearch && matchesCategory && matchesTrending;
      });
      
      // Update display
      updateDisplay();
      updateResultsCount();
      
      // Announce to screen readers
      announceToScreenReader(`Showing ${filteredApps.length} trending apps`);
    }
    
    function updateDisplay() {
      // Hide all apps
      allApps.forEach(app => {
        app.style.display = 'none';
      });
      
      // Show filtered apps
      filteredApps.forEach((app, index) => {
        app.style.display = 'block';
        app.style.order = index;
      });
    }
    
    function updateResultsCount() {
      if (showingCount && totalCount) {
        showingCount.textContent = filteredApps.length;
        totalCount.textContent = allApps.length;
      }
    }
    
    function announceToScreenReader(message) {
      const announcement = document.createElement('div');
      announcement.setAttribute('aria-live', 'polite');
      announcement.setAttribute('aria-atomic', 'true');
      announcement.className = 'sr-only';
      announcement.textContent = message;
      
      document.body.appendChild(announcement);
      
      setTimeout(() => {
        document.body.removeChild(announcement);
      }, 1000);
    }
  });
</script>

<style>
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
  
  .trending-app-card a:focus-visible {
    outline: 2px solid #ef4444;
    outline-offset: 2px;
  }
  
  @media (prefers-reduced-motion: reduce) {
    .trending-app-card,
    .trending-app-card * {
      transition: none !important;
      animation: none !important;
    }
  }
  
  /* Trending animation */
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: .5;
    }
  }
  
  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
</style>